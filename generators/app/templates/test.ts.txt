/**
 * Tests for the <%= verb %> /<%= path %> endpoint.
 *
 * Generated on <%= day %> by yo generator-serverless-concourse.
 * @author <%= username %>
 */

const mod = require('../../../functions/<%= version %>/<%= name %>/<%= handlerFile %>')

const wrapper = require('lambda-wrapper')
const reflector = require('../../../../utils/reflect')
const lambda = wrapper.wrap(reflector(mod, '<%= name %>'), { handler: '<%= handler %>' })

describe('<%= version %>/<%= name %>/<%= handlerFile %>.<%= handler %>', () => {

  beforeAll(done => {
    //
    done()
  })

  it ('should implement a test here', async () => {
    const response = await lambda.run({})
    expect(response).toBeDefined()
  })

})
