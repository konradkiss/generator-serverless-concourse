/**
 * Tests for the <%= verb %> /<%= path %> endpoint.
 *
 * Generated on <%= day %> by yo generator-serverless-concourse.
 * @author <%= username %>
 */

import * as mod from '../functions/<%= path %>/<%= handler %>'
import * as w from 'lambda-wrapper'

const lambda = w.wrap(mod, { handler: '<%= handler %>' })

describe('<%= name %>', () => {

  beforeAll(done => {
    //
    done()
  })

  it ('implements test here', async () => {
    const response = await lambda.run({})
    expect(response).toBeDefined()
  })

})
