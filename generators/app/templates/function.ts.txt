/**
 * Handler for the <%= verb %> /<%= path %> endpoint.
 *
 * Generated on <%= date %> by yo generator-serverless-concourse.
 * @author <%= username %>
 */

export const <%= name %> = (event, context, callback) => {

  const response = {
    statusCode: 200,
    body: JSON.stringify({
      message: 'Function <%= name %> executed successfully!',
      input: event,
    }),
  }

  callback(null, response)
}
