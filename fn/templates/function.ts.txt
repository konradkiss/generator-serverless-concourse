
export const <%= name %> = (event, context, callback) => {

  const response = {
    statusCode: 200,
    body: JSON.stringify({
      message: 'Function <%= name %> executed successfully!',
      input: event,
    }),
  }

  callback(null, response)
}
